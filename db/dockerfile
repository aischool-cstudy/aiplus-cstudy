FROM postgres:17-alpine

LABEL description="AI+ Study Platform — PostgreSQL Database"

# 초기화 스크립트 (알파벳 순 실행)
# 00_auth_schema.sql → auth 스키마 + auth.users 테이블
# 01_app_schema.sql  → 앱 테이블, RLS 정책, 시드 데이터
COPY init/ /docker-entrypoint-initdb.d/

EXPOSE 5432
